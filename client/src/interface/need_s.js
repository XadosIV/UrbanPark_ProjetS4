/**
 * NeedS
 * If the item is alone, returns nothing, else returns a "s" to finish trhe word
 * 
 * @param item - The item to test if there is more than 1
 * @returns String
 */

export function NeedS(item) {
    if(item > 1) {return "s";}
}
